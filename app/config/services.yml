# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true
        public: true

imports:
    - { resource: services/data.yml }
